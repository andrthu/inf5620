from numpy import *
from matplotlib.pylab import *

def mesh_function(f, t):
    sol = zeros(len(t))

    for i in range(len(t)):
        sol[i]=f(t[i])
    return sol

a=lambda x: exp(x)

t=linspace(0,4,400)

plot(t,a(t))
show()
